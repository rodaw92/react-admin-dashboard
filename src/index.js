import React from "react";
import { ReactDOM } from "react";

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // to make sure that I hooked my react application to the root div
